#include <iostream>
#include <set>

using namespace std;

int main()
{
    int numOfCase;
    cin >> numOfCase;

    for (int i = 0; i < numOfCase; i++)
    {
        multiset<int, int> gl;
        multiset<int, int> bl;

        int bf;
        cin >> bf;
        int g;
        cin >> g;
        int b;
        cin >> b;
    }
}