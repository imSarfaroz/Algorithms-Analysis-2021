#include <iostream>
#include <set>
using namespace std;

int main(void)
{
    int n;
    cin >> n;

    set<int> num;
    int numbers[1000];

    while (n--)
    {
        int a, b;
        cin >> a >> b;

        for (int i = 0; i < a; i++)
        {
            cin >> numbers[i];
        }

        // insert
        // calculate
        // print
    }
}