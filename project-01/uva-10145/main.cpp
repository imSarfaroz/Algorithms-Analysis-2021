#include <iostream>
#include <unordered_map>

using namespace std;

int main()
{
    int numOfTest;
    cin >> numOfTest;

    string line;
    getline(cin, line);

    for (int i = 0; i < numOfTest; i++)
    {
        getline(cin, line);

        while (getline(cin, line) && line != '#')
        {
            char a;
            int id, owned;
        }
    }
}